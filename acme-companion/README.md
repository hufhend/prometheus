# Automated creation/renewal of Let's Encrypt

## Basic usage

git clone https://github.com/nginx-proxy/acme-companion.git
copy docker-compose.yml into acme-companion folder
go to acme-companion and run **docker-compose up**
